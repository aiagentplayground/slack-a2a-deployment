# Slack Tokens - Get these from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# ============================================================================
# Kagent Configuration
# ============================================================================

# New to Kagent? Follow: https://kagent.dev/docs/kagent/getting-started/quickstart
# The quickstart creates a kind cluster with Kagent in the "kagent" namespace
# and sets up the default "k8s-agent"

# Option 1: Single URL (recommended for local development)
# Format: http://<host>:<port>/api/a2a/<namespace>/<agent-name>
# NOTE: No trailing slash (will auto-redirect)

# For local development with port-forward:
KAGENT_A2A_URL=http://localhost:8083/api/a2a/kagent/k8s-agent

# For in-cluster deployment:
# KAGENT_A2A_URL=http://kagent-controller.kagent.svc.cluster.local:8083/api/a2a/kagent/k8s-agent

# ============================================================================
# Option 2: Separate components (recommended for Kubernetes deployments)
# Use these if you want to pass namespace and agent name as separate env vars
# Uncomment and comment out KAGENT_A2A_URL above to use this approach
# ============================================================================

# KAGENT_BASE_URL=http://localhost:8083
# KAGENT_NAMESPACE=kagent
# KAGENT_AGENT_NAME=k8s-agent

# ============================================================================
# Default values from Kagent quickstart:
# - Namespace: kagent
# - Agent: k8s-agent
# - Port: 8083
# ============================================================================